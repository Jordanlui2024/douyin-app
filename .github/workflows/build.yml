name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build with Buildozer
      uses: uggi8/buildozer-action@v1
      with:
        buildozer-version: stable
        workdir: .
